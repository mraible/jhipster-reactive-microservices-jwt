version: '2'
services:
  jwt-mongodb:
    image: mongo:4.0.16
    ports:
      - '27017:27017'
    command: mongos --configdb jwt-mongodb-config
  jwt-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  jwt-mongodb-config:
    image: mongo:4.0.16
    container_name: jwt-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
